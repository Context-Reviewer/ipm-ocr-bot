# Timing configuration

KEY_DELAY = 0.08
MENU_DELAY = 0.12
SCROLL_DELAY = 0.18
MODULE_IDLE = 0.8

# Work chunk sizes
PLANETS_PER_TICK = 8
ORE_PAGES_PER_TICK = 1

# Scheduler intervals (seconds)
RUN_PLANETS_EVERY = 60
RUN_ORES_EVERY = 20
